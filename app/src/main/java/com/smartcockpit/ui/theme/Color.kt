package com.smartcockpit.ui.theme

import androidx.compose.ui.graphics.Color

val Background = Color(0xFFF7F4EF) // Warm, paper-like off-white
val Surface = Color(0xFFFBF9F6)    // Slightly lighter warm white
val SurfaceChat = Color(0xFFF0EBE5) 
val PrimaryText = Color(0xFF22211F)   // Soft charcoal
val SecondaryText = Color(0xFF7A7570) // Muted taupe
val AccentColor = Color(0xFFD4A373)   // Classic sand
val BorderColor = Color(0x1A000000)   // Subtle border (10% alpha)
val DividerColor = Color(0x0D000000)  // Faint divider
val CompletedTaskText = Color(0xFFAFA9A4)
